#include <bits/stdc++.h>
using namespace std;

#ifdef  LOCAL
#define D(x) cerr << #x << " = " << x << endl;
#else
#define D(x)
#endif

void solve() {
  int t; cin >> t;

  while (t--) {
    int n; cin >> n;
    int k; cin >> k;

    if (k > n) {
      cout << "NO\n";
      continue;
    }
  }
}

int main() {
  cin.tie(0); cout.tie(0);
  ios::sync_with_stdio(0);
  solve();
  return 0;
}

